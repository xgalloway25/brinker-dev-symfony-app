<form method="post" action="{{ path('app_reward_delete', {'id': reward.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reward.id) }}">
    <button type="button" class="btn btn-danger">Delete</button>
</form>
